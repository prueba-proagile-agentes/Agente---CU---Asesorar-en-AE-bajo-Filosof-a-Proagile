schema_version: 2

agent:
  id: generador
  role: generador
  instructions: AGENTS.md
  context: CONTEXT.md

inherits:
  project_config: ../../agent-config.yaml
  project_skills: true
  model_by_role: true

output:
  mode: deliverable
  requires_review: true