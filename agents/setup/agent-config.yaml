schema_version: 2

agent:
  id: setup
  role: setup
  instructions: AGENTS.md
  context: CONTEXT.md

inherits:
  project_config: ../../agent-config.yaml
  project_skills: true
  model_by_role: true

output:
  mode: guidance
  requires_review: false