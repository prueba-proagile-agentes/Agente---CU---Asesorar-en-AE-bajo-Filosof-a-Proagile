schema_version: 2

agent:
  id: revisor
  role: revisor
  instructions: AGENTS.md
  context: CONTEXT.md

inherits:
  project_config: ../../agent-config.yaml
  project_skills: true
  model_by_role: true

output:
  mode: review-findings
  requires_review: false